

const antiShake = function(){
    const timerSignCatch = {};
    return (func,deferTime,context) => {
        const timeid = setTimeout(() => {
            if (context) { func. }
        }, deferTime);
        if (timerSignCatch[timeid]) {  }
    }
}

export default antiShake;
